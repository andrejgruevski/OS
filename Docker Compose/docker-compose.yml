version: '3'

services:
  web:
    image: nginx:alpine

    ports:
      - "80:80"

    volumes:
      - ./frontend:/usr/share/nginx/html

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile

    ports:
      - "8080:8080"